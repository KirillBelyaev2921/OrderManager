spring:
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://localhost:5432/order_manager
    username: products_managing_owner
    password: 1234
    driverClassName: org.postgresql.Driver
  sql:
    init:
      platform: postgres